# Legacy Python Desktop

Questa cartella contiene il codice desktop storico del progetto.

Non fa parte del deploy Vercel del sito web.

Contenuto:

- GUI `customtkinter`
- core Python storico per formule e percorsi
- launcher `.bat`
- dipendenze in `requirements-desktop.txt`

Per il prodotto attivo usare invece:

- `Cicli-Termodinamici-Web/`
